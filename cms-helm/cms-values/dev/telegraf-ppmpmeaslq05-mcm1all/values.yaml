deployment:
  enabled: true
  replicas: 0
  image:
    repository: image-registry.openshift-image-registry.svc:5000/ci-dcmlp-openshift-cms-dev/dcmlp-cm-telegraf
    tag: latest
  ports:
    - name: listener
      containerPort: 8080
  volumeMounts:
    - name: config-volume
      mountPath: /etc/telegraf/telegraf.conf
      subPath: telegraf.conf
      readOnly: true
  volumes:
    - name: config-volume
      configMap:
        name: dcmlp-cms-dev-telegraf-ppmpmeaslq05-mcm1all-config
  env:
    - name: INFLUX_TOKEN
      valueFrom:
        secretKeyRef:
          name: dcmlp-cms-dev-influxdb-secret
          key: INFLUXDB_TOKEN_ALL_BUCKETS_WRITE
  resources:
    limits:
      cpu: 20m
      memory: 150Mi
    requests:
      cpu: 10m
      memory: 50Mi

service:
  enabled: false

route:
  enabled: false

configmap:
  enabled: true
  name: dcmlp-cms-dev-telegraf-ppmpmeaslq05-mcm1all-config
  data:
    telegraf.conf: |
      [agent]
        interval = "10s"
      [[outputs.influxdb_v2]]
        urls = ["http://dcmlp-cms-dev-influxdb:8086"]
        token = "$INFLUX_TOKEN"
        organization = "dcmlp"
        bucket = "mcm1all"
      [[inputs.opcua]]
        endpoint = "opc.tcp://ppmpmeaslq05:4840"
        nodes = [{name="mcm1all", namespace="1", identifier_type="s", identifier="mcm1.all"}]
